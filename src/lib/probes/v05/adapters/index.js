// src/lib/probes/v05/adapters/index.js
//
// Language-specific adapter records aggregated for the manifest. Phase 0
// ships with zero adapters. As each adapter lands under adapters/<lang>/,
// import it here and add to the exported array.

/** @type {object[]} */
export const ADAPTERS = [];
